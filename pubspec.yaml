name: dimengen
description: "Flutter Dimensions Generator"
version: 0.1.0
homepage: "https://github.com/warioddly/dimengen"


environment:
  sdk: ^3.8.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  source_gen: ^2.0.0
  build: ^2.4.2
  analyzer: ^7.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  topics:
    - dimensions
    - generator
    - dimengen

platforms:
  linux:
  macos:
  windows: