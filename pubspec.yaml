name: dimengen
description: "Flutter Dimensions Generator (it helps you centralize spacing and size values, improve UI consistency, and boost code readability and maintainability)"
version: 0.6.0
homepage: "https://github.com/warioddly/dimengen"

environment:
  sdk: '>3.2.0 <=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  source_gen: ^4.0.1
  build: ^4.0.0
  analyzer: 8.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.7.2

topics:
  - dimensions
  - generator
  - sizes
  - uikit
  - gen

platforms:
  linux:
  macos:
  windows:
